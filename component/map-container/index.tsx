import {Vue, Component} from 'vue-property-decorator'

import './index.postcss'
@Component({name:"ssui-map-container"})
export default class SsHeader extends Vue{
  render(){
    return <div>我是map</div>
  }
}